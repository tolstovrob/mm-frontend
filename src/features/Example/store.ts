/**
 * `store.ts` contains storage that feature use. You also can handle localStorage, sessionStorage, cookies and indexDB there.
 * For default stores you can use writables from svelte, but make sure you cache it properly: it will recalculate itself on reload
 */
